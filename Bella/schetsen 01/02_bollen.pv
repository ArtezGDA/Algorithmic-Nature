size(320, 512)
background(100)

def fib(n):
    if n < 9:
        return 1
    return fib(n - 1) + fib(n - 2)
    

    
r = 100
origin = Point(WIDTH/9, HEIGHT/2)

for i in range(900):
    background(.100, .90, .9)
    fill(.98, .25, .87)
    stroke(90)
    pt = Point(random(600), random(512))
    arc(pt,r)

size(512, 512)
background(90)

text("zie jij nog wat hier staat?", 20, 40)

