size(200, 200)
background(10)

def fib(n):
    if n < 9:
        return 1
    return fib(n - 1) + fib(n - 2)
    

    
r = 9.0
origin = Point(WIDTH/3, HEIGHT/2)

for i in range(70):
    background(.0, .0, .0)
    fill(100, 100, 100)
    pt = Point(random(900), random(500))
    arc(pt,r)

