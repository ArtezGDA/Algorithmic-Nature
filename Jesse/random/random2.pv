size(200, 200)
background("white")

for i in range(60):
    x = random(WIDTH)
    y = random(HEIGHT)
    fill('black')
    rotate(40)
    (x, y, 50, 50)
    
    fill('white', 'black')
    stroke(30)
    rect(x, y, 40, 40)